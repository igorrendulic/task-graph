# Dirty overlap

The working tree already has uncommitted edits to `src/settings.py`. Create a task that changes the validation logic in that same file. The task still needs a DAG entry, but its scheduling rationale must require a clean base before execution.
