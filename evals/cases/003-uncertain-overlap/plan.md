# Uncertain overlap

Add a cache invalidation task. Add a second task to update the integration boundary, but the repository inspection cannot establish which implementation files the integration update changes. Preserve source-plan order because the second task's edit surface is uncertain.
