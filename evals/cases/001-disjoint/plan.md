# Disjoint changes

Create one task that adds `src/schema.py` and its focused test, and one task that adds `src/formatter.py` and its focused test. The modules have no shared imports, contracts, generated artifacts, or test files.
