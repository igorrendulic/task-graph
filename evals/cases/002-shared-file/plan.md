# Shared file

First add a parser to `src/config.py`. Then add a serializer to the same `src/config.py` module. Neither task is a natural prerequisite of the other, so preserve this source-plan order when scheduling.
